# Framework Backlog

Living document. When a reusable capability or framework improvement is spotted
mid-milestone, it gets recorded here instead of implemented on the spot.

**Format:** each entry has the idea, why it's valuable, a priority (P1 = high leverage /
soon, P2 = solid value, P3 = nice to have), and the recommended milestone.

| #   | Idea                                                                       | Why it's valuable                                                                                                                        | Priority | Target                                  |
| --- | -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------- |
| 1   | LazyMotion + `m` components instead of full `motion`                       | Cuts ~25 kB of Framer Motion from every page's first load; helps hold Lighthouse ≥95 as pages grow                                       | P1       | ✅ Shipped M13                          |
| 2   | Strict CSP with per-request nonces                                         | Completes the security-header story; needs middleware nonce plumbing, so it's real work — do it once, deliberately                       | P1       | ✅ Shipped M13 (`lib/security/csp.ts`)  |
| 3   | Upstash Redis adapter for `lib/rate-limit.ts`                              | In-memory limiter is per-instance; multi-region Vercel deployments need shared state. Call-site API already matches                      | P2       | ✅ Shipped M14 (fetch-based, no SDK)    |
| 4   | User invites from admin (`auth.admin.inviteUserByEmail`)                   | Lets clients onboard staff without sign-up links; requires adding the service-role key to server env (deliberate security decision)      | P2       | M5 or M6                                |
| 5   | Admin audit log (who changed what, when)                                   | Premium trust feature for multi-editor client teams; capability-friendly (one table + a `logAction()` helper in server actions)          | P2       | M10                                     |
| 6   | Zod schemas for section props (section registry contract)                  | Already planned as part of M5 — recorded here so it isn't lost: schemas drive CMS validation AND the page-builder UI                     | P1       | M5                                      |
| 7   | Breadcrumb JSON-LD generator                                               | Cheap SEO win once breadcrumbs exist in admin/public pages; belongs with the other JSON-LD generators                                    | P2       | M7                                      |
| 8   | Re-add shadcn CLI flow docs (registry was unreachable from sandbox)        | Hand-written primitives match shadcn spec; document that future components can come from `npx shadcn add` on a dev machine               | P3       | M14                                     |
| 9   | i18n readiness (next-intl or per-client locale config)                     | Some future clients will want ES/other locales; retrofitting is painful. Decide the pattern before M12 modules multiply page count       | P2       | M12 (decision), impl. when first needed |
| 10  | Image placeholder pipeline (blur hashes for CMS uploads)                   | Perceived-performance polish for gallery-heavy industries; generate blurDataURL at upload time in the media library                      | P2       | M5 (media library) or M8                |
| 11  | E2E smoke tests (Playwright) for auth + admin critical paths               | Verification gates catch compile-time issues; a 5-test Playwright suite catches runtime regressions before client deploys                | P1       | ✅ Shipped M14 (`tests/smoke.spec.ts`)  |
| 12  | Per-client theme presets (token bundles: "warm", "mono", "bold")           | Speeds up the rebrand step from "pick 20 oklch values" to "pick a preset, tweak two"                                                     | P2       | M8                                      |
| 13  | Visual page builder v2 (form-per-section, drag to reorder, live preview)   | Page editor v1 validates JSON blocks against registry schemas — correct but developer-ish. Zod schemas can auto-generate the field forms | P1       | M8                                      |
| 14  | Image node in rich text editor (insert from media library)                 | Post bodies are text-only today; cover image + media library exist, so wiring an image extension into Tiptap is a small step             | P2       | M8                                      |
| 16  | Gallery albums admin UI (curated albums vs. raw media feed)                | Schema shipped in 0005; /gallery currently shows the media library. Album curation is the premium touch for portfolio industries         | P2       | M12                                     |
| 17  | Sort-order drag & drop in CrudManager                                      | Numeric sort_order works but feels developer-ish; dnd-kit on the shared CRUD benefits every capability at once                           | P3       | M13+                                    |
| 18  | Memberships/loyalty capability (recurring plans, punch cards)              | Nail salons, med spas, HVAC plans, coffee loyalty all want it; today they present as promotions. Needs Stripe subscriptions              | P2       | post-M14 (first client who needs it)    |
| 19  | Live schedule capability (food-truck daily tracker)                        | Locations capability covers it manually; a dated schedule table + map embed is the polished version                                      | P3       | post-M14                                |
| 15  | Draft preview links (signed URL to view unpublished content)               | Editors currently publish to see the real page; a tokenized preview route is the professional workflow                                   | P1       | M6 or M8                                |
| 20  | Error-tracking seam (Sentry/Axiom behind `lib/log.ts`)                     | Structured logger is the single place to wire a real error tracker; error boundaries already emit digests                                | P2       | first client that pays for monitoring   |
| 21  | Google Places API review sync (per-location live rating + reviews)         | Manual curation ships in M17; live sync is the upsell — needs Google Cloud key, quota handling, and cache-with-revalidate                | P2       | post-M20 (first client who demands it)  |
| 22  | Admin drag-and-drop form builder (DB-stored form schemas)                  | Config-defined forms ship in M16; a builder lets clients self-serve new forms — big build, validate demand first                         | P3       | post-M20                                |
| 23  | Elegant + Medical themes (and remaining theme list)                        | Token engine (M15) makes each new theme a definition file + font pairing + dual-mode QA                                                  | P2       | post-M15, as client work demands        |
| 24  | SMS notifications for emergency-service forms (Twilio seam)                | M16 ships the "loud path" seam; wiring an SMS provider is a per-client decision with its own billing                                     | P3       | first emergency-trades client           |
| 25  | Inline AI in editors (Tiptap toolbar: rewrite selection, continue writing) | AI Studio ships copy/paste flows in M19; inline generation inside the post/page editors is the polish step                               | P2       | post-M20                                |
